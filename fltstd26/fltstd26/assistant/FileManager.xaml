<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="fltstd26.assistant.FileManager"
             xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
             Title=""
             Shell.NavBarIsVisible="false"
             x:DataType="Color"
             xmlns:local="clr-namespace:fltstd26.Resources.Texts">
    <Grid RowDefinitions="Auto,*,Auto"
              VerticalOptions="Fill">
        <Border BackgroundColor="{x:AppThemeBinding Dark={StaticResource Gray950}, Light={StaticResource Gray300}}"
                        Padding="20"
                        StrokeThickness="0">
            <Label FontSize="28"
                       FontFamily="Archive"
                       VerticalOptions="Center"
                   x:Name="WindowTitle"
                       HorizontalOptions="Start"/>
        </Border>
        <Border Grid.Row="1"
                    VerticalOptions="Fill"
                    StrokeThickness="0">
            <Grid    RowSpacing="12"
                     RowDefinitions="Auto,*"
                                        VerticalOptions="Fill"
                                        Padding="20">
                <Label FontSize="22"
                       VerticalOptions="Center"
                       x:Name="WindowSubtitle"
                       HorizontalOptions="Start"/>
                <Border StrokeThickness="2"
                        Padding="10"
                        Grid.Row="1"
                        VerticalOptions="Fill"
                        BackgroundColor="{x:AppThemeBinding Dark={StaticResource Gray950}, Light={StaticResource Gray300}}">
                    <ScrollView>
                        <VerticalStackLayout x:Name="FileView"
                                             Spacing="10"/>
                    </ScrollView>
                </Border>
            </Grid>
        </Border>

        <Border Background="{AppThemeBinding Light={StaticResource SecondaryDark}, Dark={StaticResource SecondaryDark}}"
                            HorizontalOptions="Fill"
                    VerticalOptions="End"
                    Grid.Row="2"
                    Padding="10"
                    StrokeThickness="0">
            <Grid ColumnDefinitions="*,*,*,*,*">
                <Button Text="{x:Static local:Lang.newobj}"
                                    Clicked="PlusClick"
                                    TextColor="Black"
                                    Grid.Column="0"
                                    CornerRadius="0"
                                    BackgroundColor="Transparent"
                                    FontAttributes="Bold"
                                    ImageSource="plus.png"/>
                <Button Text="{x:Static local:Lang.refresh}"
                                    Clicked="RefreshClick"
                                    TextColor="Black"
                                    Grid.Column="1"
                                    CornerRadius="0"
                                    BackgroundColor="Transparent"
                                    FontAttributes="Bold"
                                    ImageSource="refresh.png"/>
                <Button Text="{x:Static local:Lang.remote_continue}"
                                    Clicked="RemoteClick"
                            x:Name="RemoteButton"
                                    TextColor="Black"
                                    Grid.Column="2"
                                    CornerRadius="0"
                                    BackgroundColor="Transparent"
                                    FontAttributes="Bold"
                                    ImageSource="cast.png"/>
                <Button Text="{x:Static local:Lang.explorer_continue}"
                                    Clicked="OpenClick"
                                    TextColor="Black"
                                    Grid.Column="3"
                                    CornerRadius="0"
                                    BackgroundColor="Transparent"
                                    FontAttributes="Bold"
                                    ImageSource="archive.png"/>
                <Button Text="{x:Static local:Lang.assistant_continue}"
                                    Clicked="ContinueClick"
                                    TextColor="Black"
                                    Grid.Column="4"
                                    CornerRadius="0"
                                    BackgroundColor="Transparent"
                                    FontAttributes="Bold"
                                    ImageSource="play.png"/>
            </Grid>
        </Border>
    </Grid>
</ContentPage>