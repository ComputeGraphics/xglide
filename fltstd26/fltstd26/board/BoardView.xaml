<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:uset="clr-namespace:fltstd26.etc"
             x:Class="fltstd26.board.BoardView">
    <Grid Padding="10"
          BackgroundColor = "{AppThemeBinding Dark={StaticResource Gray900}, Light={StaticResource Gray100}}"
          ColumnDefinitions="80, *">
        <Grid ColumnDefinitions="Auto,Auto"
              VerticalOptions="Center"
              HorizontalOptions="Center"
              ColumnSpacing="5">
            <Label Text="●"
               x:Name="GreenLight"
               TextColor="DarkGray"
               FontSize="{Binding Source={x:Static uset:USettings.FlashSize}}"/>
            <Label Text="●"
               x:Name="RedLight"
               Grid.Column="1"
               TextColor="DarkGray"
               FontSize="{Binding Source={x:Static uset:USettings.FlashSize}}"/>
        </Grid>
        <Grid x:Name="ColumnContainer"
              VerticalOptions="Center"
              HorizontalOptions="Start"
              Grid.Column="1"/>
    </Grid>
</ContentView>
